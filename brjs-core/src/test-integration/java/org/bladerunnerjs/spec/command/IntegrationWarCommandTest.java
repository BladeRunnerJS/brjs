package org.bladerunnerjs.spec.command;

public class IntegrationWarCommandTest {
	// TODO: this test currently has to live within 'old-bladerunner-tests' given that we still rely on old bundler code in production -- move it back in once this is no longer the case
}
