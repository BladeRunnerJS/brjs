package org.bladerunnerjs.spec.brjs;

// TODO: delaying creating any tests for limited test-runner.conf support we have so far,
// since the current config file is wrong anyway, as it contains plugin specific (js-test-driver)
// config, so it's worth waiting till we start working on test plugins before addressing it
public class TestRunnerConfTest {
}
